const raio = 2.5;
const area = Math.PI * Math.pow(raio, 2); // "Math.PI = 3.14159"

console.log(area.toFixed(2));
console.log(typeof Math);

/* 
    JS possui uma série de objetos dentro dele para te auxiliar. Um deles,
    é o objeto "Math", que é utilizado para representar uma operação aritmética
    de exponenciação, já que o JS não possui um operador exclusivo para isso.
*/