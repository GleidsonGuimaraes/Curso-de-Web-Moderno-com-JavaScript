// Declaração de variáveis

var a = 3;
let b = 4; 

/* Sintaxe <identificador nome = valor>

Procure sempre utilizar o identificador <let>

*/

var a = 30;
/*let*/ b = 40; 

// O identificador <let> só pode ser declarado uma vez.
// Ambas as variáveis, não precisam que o identificador seja declarado mais de uma vez.

console.log (a, b);

//Declaração de constante

const c = 5; // Uma constante não pode ter seu valor alterado.

console.log(c);

